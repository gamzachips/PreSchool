using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Layser : MonoBehaviour
{
    [SerializeField]
    float persistTime = 0.3f;
    public float PersistTime {  get { return persistTime; }  }

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
